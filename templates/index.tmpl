<html>
	<h1>
		{{ .title }}
	</h1>

    <table class="table">
        <tbody>
        {{range $index, $_ := .data}}
        <tr>
            <td width="30%">{{$index}}</td>
            <td>{{.}}</td>
        </tr>
        {{end}}
        </tbody>
    </table>

</html>
